import { ThbCurrencyPipe } from './thb-currency.pipe';

describe('ThbCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ThbCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
