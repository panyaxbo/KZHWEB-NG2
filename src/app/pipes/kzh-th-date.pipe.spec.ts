import { KzhThDatePipe } from './kzh-th-date.pipe';

describe('KzhThDatePipe', () => {
  it('create an instance', () => {
    const pipe = new KzhThDatePipe();
    expect(pipe).toBeTruthy();
  });
});
